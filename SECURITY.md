# 보안 정책

## 지원 버전

| 버전 | 상태 |
|---|---|
| 0.1.x | 사전릴리즈, 보안 수정 대상 |
| 그 이전 | 지원하지 않음 |

## 취약점 제보

가능하면 GitHub의 **Private vulnerability reporting** 기능을 사용하십시오.
공개 Issue, 토론 또는 스크린샷에 다음 정보를 넣지 마십시오.

- 실제 장비 IP/호스트명, 고객명 또는 네트워크 구성
- 사용자 이름, 암호, Enable 암호, 개인 키 또는 토큰
- `running-config`, 장비 전체 출력 또는 승인된 호스트 키 목록

제보에는 민감정보를 제거한 영향 범위, 재현 단계, 예상/실제 결과, 사용한
애플리케이션 버전과 Windows 버전을 포함해 주십시오. 실제 설정 대신
RFC 5737 문서용 주소와 최소 합성 출력을 사용하십시오.

유지관리자는 접수 확인 후 공개 범위와 수정 일정을 제보자와 조율합니다.
보안 수정이 배포되기 전에는 상세 재현 정보를 공개하지 마십시오.

## 운영 안전 경계

이 프로그램은 읽기 전용 백업 도구이지만 다음 사항을 운영자가 직접
확인해야 합니다.

- 최초 SSH 호스트 키 지문을 신뢰할 수 있는 별도 경로로 대조합니다.
- `HOST_KEY_CHANGED`를 우회하거나 저장 파일을 임의로 덮어쓰지 않습니다.
- `main`은 인증 전 지문 확인과 실제 인증 연결 모두에서 RSA/SHA-1 서명 방식인
  `ssh-rsa`를 차단합니다. 이 방식만 제공하는 장비에는 자격증명을 보내지 않고
  `SSH_ALGORITHM_INCOMPATIBLE`로 종료합니다.
- 대상 장비가 RSA-SHA2, ECDSA 또는 Ed25519 서버 키를 제공하도록 펌웨어와 SSH
  설정을 갱신합니다. 보안 설정을 낮추는 자동 대체 경로는 제공하지 않습니다.
- 결과 TXT와 Excel은 네트워크 민감정보로 분류하고 접근을 제한합니다.
- 사전릴리즈를 실제 환경에 배포하기 전에 대표 2930F에서 검증합니다.
- 서명되지 않은 EXE는 게시된 SHA-256과 GitHub Actions 출처를 확인합니다.

이 저장소의 자동 테스트, 가짜 SSH 서버, 패키지 smoke는 실제 장비나 실제
회사 네트워크에서의 안전성을 입증하지 않습니다.

## 승인된 의존성 감사 예외

| ID | 대상 | 사유와 보완 통제 | 제거 조건 |
|---|---|---|---|
| `PYSEC-2026-2858` / `CVE-2026-44405` | Paramiko 4.0.0의 RSA/SHA-1 허용, 5.0.0 후보의 업그레이드 검증 미완료 | 호스트 키 사전점검과 인증 연결에 동일한 `ssh-rsa` 차단 정책을 적용하고, 변경된 장비 키를 계속 차단함 | 수정 버전으로 잠금 파일을 갱신하고 동일 회귀 테스트를 통과할 때 |

검증 스크립트는 위 CVE 한 건만 `pip-audit`에서 제외합니다. 경고를 해결됨으로
표시하거나 범위를 넓히지 않으며, 상세 근거와 2026-09-24 재검토 기한은
[의존성 감사 예외](docs/DEPENDENCY_AUDIT_EXCEPTIONS_KO.md)에 기록합니다.
